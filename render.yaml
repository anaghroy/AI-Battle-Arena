services:
  - type: web
    name: ai-battle-arena
    env: docker
    plan: free
    dockerContext: .
    dockerfilePath: ./Dockerfile
    branch: main
    autoDeploy: true
    envVars:
      - key: PORT
        value: 3000
