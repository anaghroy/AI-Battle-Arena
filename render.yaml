services:
  - type: web
    name: ai-battle-arena
    env: docker
    plan: free
    dockerContext: .
    dockerFilePath: Dockerfile
    autoDeploy: true
    envVars:
      - key: PORT
        value: 3000
